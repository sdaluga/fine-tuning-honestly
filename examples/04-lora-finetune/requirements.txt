# Training dependencies. NOT installed in CI — this example needs a GPU, and
# a training job that runs on every push is a bill, not a test.
#
# The toolkit in src/ deliberately depends on none of this. That is what lets
# the decision, curation, evaluation and governance layers be tested in
# milliseconds on any machine.

torch>=2.4.0
transformers>=4.44.0
peft>=0.13.0
trl>=0.11.0
datasets>=3.0.0
accelerate>=0.34.0
bitsandbytes>=0.44.0    # 4-bit base weights (QLoRA)
pyyaml>=6.0
